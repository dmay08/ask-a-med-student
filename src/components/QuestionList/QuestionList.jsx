import React from 'react';

const QuestionList = (props) => (
    <div className="QuestionList">
        <h1>My Questions:</h1>
        {/* {props.questionList.map((question, idx) => <Question key={index} question={question} /> )}  */}
    </div>
)

export default QuestionList;